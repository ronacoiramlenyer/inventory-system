# Only needed for a production build targeting the deployed Cloudflare Worker.
# Leave unset for local dev (uses the Vite proxy to http://localhost:4000/api).
VITE_API_URL=https://inventory-system-api.<your-subdomain>.workers.dev/api
